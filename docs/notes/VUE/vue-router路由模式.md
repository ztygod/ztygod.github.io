## Hash模式

`hash` 模式是一种把前端路由的路径用井号 `#` 拼接在真实 `url` 后面的模式。当井号 `#` 后面的路径发生变化时，浏览器并不会重新发起请求，而是会触发 `onhashchange` 事件。

## History模式

`histroy` 和正常 url 路径一样，但是需要在服务器进行单独配置。简单来讲就是，`hash` 路由兼容梗好，但是带#显得丑些， `histroy` 和正常 url 路径一样，但是需要在服务器进行单独配置。